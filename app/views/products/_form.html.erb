<%= form_for(@product, :html => { :multipart => true}) do |f|%>
  <% if product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title, id: :product_title %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description, id: :product_description %>
  </div>

  <div class="field">
    <%= f.label :photo %>
    <%= f.file_field :photo, id: :product_image %>
  </div>

  <div class="field">
    <%= f.label :suburb %>
    <%= f.text_field :suburb, id: :product_suburb %>
  </div>

  <div class="field">
    <%= f.label :category_id %>
    <%= f.number_field :category_id, id: :product_category_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
